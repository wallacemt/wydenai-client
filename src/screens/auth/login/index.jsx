import React from "react";
import LoginForm from "../../../components/auth/loginForm";

const LoginScreen = () => {
    return (
        <>
            <LoginForm />
        </>
    );
};

export default LoginScreen;
